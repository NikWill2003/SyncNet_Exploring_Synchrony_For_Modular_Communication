# @package _global_

defaults:
  - /model: sort_of_clevr/transformer
  - _self_
model:
  encoder:
    ch: 128
hydra:
  mode: MULTIRUN
  sweeper:
    params:
      model: sort_of_clevr/transformer,sort_of_clevr/shared_workspace
      model.encoder.name: patchify,cnn
      model.n_layers: 2,4
      train.seed: 0,1
