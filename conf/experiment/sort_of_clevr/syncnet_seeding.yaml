# @package _global_

defaults:
  - /model: sort_of_clevr/syncnet
  - _self_

callbacks: [freeze_phase, zero_phase, shuffle_phase]

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      model.bias_init: zero,partition,random
      train.seed: 0,1,2,3,4
