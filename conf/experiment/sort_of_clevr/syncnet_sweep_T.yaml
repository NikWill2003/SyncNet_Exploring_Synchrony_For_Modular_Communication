# @package _global_

defaults:
  - /model: sort_of_clevr/syncnet
  - _self_

callbacks: [freeze_phase, zero_phase, shuffle_phase]

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      model.t_bus: 2,4,16
      train.seed: 0,1,2
