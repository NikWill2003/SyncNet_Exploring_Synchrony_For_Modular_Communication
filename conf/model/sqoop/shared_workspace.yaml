defaults:
  - shared_workspace_base
  - _self_

name: shared_workspace

q_encoder:
  name: tokenise
  embedding_dim: 32

mem_slots: 8

encoder:
  name: patchify
  ch: 256
  patch_size: 4
  
n_layers: 4
